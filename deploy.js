const {resolve} = require('path');
const fs = require('fs').promises;

// percorrer toda a arvore de arquivos 
// pegar o nome de cada arquivo 
// pegar a localização do diretório daquele arquivo
// pegar lista de localização 
// percorrer a lista
// entrar nos arquivos, um por um 
// verificar se há "require(dotenv).config();
// Se houver "require('dotenv') > remover linha, se não > passa pro próximo"

